services:
  - type: web
    name: dayedge-scanner
    env: python
    runtime: python311
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    envVars:
      - key: ALPACA_API_KEY
        sync: false
      - key: ALPACA_API_SECRET
        sync: false
      - key: NEWS_API_KEY
        sync: false
```

5. Click **"Commit changes"**

---

**Fix 2 — Update package versions in requirements.txt:**

1. Click on `requirements.txt` in your repo
2. Click the pencil icon to edit
3. Replace everything with this:
```
flask==3.0.0
alpaca-py==0.26.0
pandas==2.1.4
numpy==1.26.4
requests==2.31.0
APScheduler==3.10.4
gunicorn==21.2.0
